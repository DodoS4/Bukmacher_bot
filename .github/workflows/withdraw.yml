name: Wypłata pieniędzy 💸

on:
  workflow_dispatch:
    inputs:
      amount:
        description: 'Kwota do wypłaty (np. 500)'
        required: true
        default: '0'

jobs:
  withdraw:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run Withdrawal Script
        env:
          T_TOKEN: ${{ secrets.T_TOKEN }}
          T_CHAT: ${{ secrets.T_CHAT }}
        run: |
          # Uruchamiamy skrypt i przekazujemy kwotę jako argument
          python withdraw.py "${{ github.event.inputs.amount }}"

      - name: Commit and Push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

          # Dodajemy oba kluczowe pliki do bazy zmian
          git add history.json stats.json

          # Sprawdzamy czy faktycznie coś się zmieniło przed wysłaniem
          if git diff --staged --quiet; then
            echo "ℹ️ Brak zmian w plikach JSON – pomijam aktualizację strony."
          else
            git commit -m "💸 Rejestracja wypłaty: ${{ github.event.inputs.amount }} PLN"
            git push
          fi
