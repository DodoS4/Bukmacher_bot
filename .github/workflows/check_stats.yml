import os
import sys
from datetime import datetime, timezone
from main_bot import send_stats  # zakładam, że Twój główny bot jest w main_bot.py / start.py

if __name__ == "__main__":
    print("Generowanie statystyk ręcznie...")
    send_stats()
    print("Raport wysłany!")